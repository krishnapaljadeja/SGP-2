<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>QuiziFY - OTP Verification</title>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined" rel="stylesheet">
    <style>
        :root {
            --primary-gradient: linear-gradient(135deg, #6366f1, #8b5cf6);
            --secondary-gradient: linear-gradient(135deg, #3b82f6, #2dd4bf);
            --background-gradient: linear-gradient(135deg, #f8fafc, #e2e8f0);
            --card-shadow: 0 10px 20px rgba(0, 0, 0, 0.1);
            --hover-transform: translateY(-5px);
        }

        * {
        margin: 0;
        padding: 0;
            box-sizing: border-box;
            font-family: 'Poppins', sans-serif;
      }
      body {
            background: var(--background-gradient);
            min-height: 100vh;
            display: flex;
        justify-content: center;
            align-items: center;
            color: #1e293b;
      }

        .otp-container {
            width: 100%;
            max-width: 420px;
            padding: 2rem;
        }

        .otp-card {
            background: white;
            border-radius: 20px;
            padding: 2.5rem;
            box-shadow: var(--card-shadow);
            transition: all 0.3s ease;
            border: 1px solid rgba(0, 0, 0, 0.1);
        }

        .otp-card:hover {
            transform: var(--hover-transform);
            box-shadow: 0 15px 30px rgba(0, 0, 0, 0.15);
        }

        .logo {
            font-size: 2.5rem;
            font-weight: 700;
            background: var(--primary-gradient);
            -webkit-background-clip: text;
            background-clip: text;
            color: transparent;
            letter-spacing: -1px;
            text-align: center;
            margin-bottom: 2rem;
        }

        .icon-container {
            text-align: center;
            margin-bottom: 1.5rem;
        }

        .icon-container .material-symbols-outlined {
            font-size: 3rem;
            background: var(--primary-gradient);
            -webkit-background-clip: text;
            background-clip: text;
            color: transparent;
        }

        .title {
            text-align: center;
            font-size: 1.5rem;
            font-weight: 600;
            margin-bottom: 1rem;
            color: #1e293b;
        }

        .description {
            text-align: center;
            color: #64748b;
            margin-bottom: 2rem;
        }

        .otp-input {
            width: 100%;
            padding: 1rem;
            border: 1px solid #e2e8f0;
            border-radius: 12px;
            background: #f8fafc;
            font-size: 1.2rem;
            text-align: center;
            letter-spacing: 0.5rem;
            margin-bottom: 1.5rem;
            transition: all 0.3s ease;
        }

        .otp-input:focus {
            outline: none;
            border-color: #6366f1;
            background: white;
            box-shadow: 0 0 0 4px rgba(99, 102, 241, 0.1);
        }

        .verify-btn {
            width: 100%;
            padding: 1rem;
            border: none;
            border-radius: 12px;
            background: var(--primary-gradient);
            color: white;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s ease;
            margin-bottom: 1.5rem;
        }

        .verify-btn:hover {
            transform: translateY(-2px);
            box-shadow: 0 5px 15px rgba(99, 102, 241, 0.3);
        }

        .resend-container {
            display: flex;
            justify-content: center;
            align-items: center;
            gap: 0.5rem;
            margin-bottom: 1rem;
        }

        .resend-text {
            color: #64748b;
        }

        .resend-link {
            color: #6366f1;
            text-decoration: none;
            font-weight: 500;
            cursor: pointer;
            transition: color 0.3s ease;
        }

        .resend-link:hover {
            color: #818cf8;
        }

        .timer-container {
            display: flex;
            justify-content: center;
            align-items: center;
            gap: 0.5rem;
            color: #64748b;
        }

        .timer-container .material-symbols-outlined {
            font-size: 1.2rem;
        }

        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(10px); }
            to { opacity: 1; transform: translateY(0); }
        }

        .fade-in {
            animation: fadeIn 0.5s ease-out;
        }
    </style>
  </head>
<body>
    <div class="otp-container">
        <form action="/otp" method="post" class="otp-card fade-in">
            <h1 class="logo">QUIZIFY</h1>
            
            <div class="icon-container">
                <span class="material-symbols-outlined">lock</span>
                </div>
    
            <h2 class="title">OTP Verification</h2>
            <p class="description">Enter the verification code we sent to your email address</p>

            <input type="text" 
                   name="otp" 
                   class="otp-input" 
                   maxlength="6" 
                   placeholder="Enter OTP"
                   required>

            <button type="submit" class="verify-btn">
                Verify OTP
            </button>
           
    
            <div class="timer-container">
                <span class="material-symbols-outlined">timer</span>
                <span>Code expires in 2 minutes</span>
            </div>
        </form>
    </div>
</body>
</html>